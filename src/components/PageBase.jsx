import { Box, styled } from "@mui/material";

export const PageBase = styled(Box)(() => ({
    maxWidth: 1035,
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
}))